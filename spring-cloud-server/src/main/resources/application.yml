server:
  port: 8761
  servlet:
    context-path: /springCloudServer

eureka:
  instance:
    hostname: peer1
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://peer2:8773/springCloudServer1/eureka/

spring:
  application:
    name: spring-cloud-server
